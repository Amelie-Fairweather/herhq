[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# After first deploy: Railway → Service → Volumes → Add Volume
# Mount path: /data
# Then set variable DATA_DIR=/data so accounts survive redeploys.
