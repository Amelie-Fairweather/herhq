[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["mkdir -p data/reports/current data/reports/archive", "npm run build"]

[start]
cmd = "mkdir -p \"${DATA_DIR:-data}/reports/current\" \"${DATA_DIR:-data}/reports/archive\" && npm run start"
